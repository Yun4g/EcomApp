Great question — this is exactly how real-world backends protect routes 🔐
I’ll show you a clean, production-style RBAC (Role-Based Access Control) setup using Node.js + Express + TypeScript.

🧠 Concept (Simple)

You want to:

Authenticate user (who are you?) → via JWT
Authorize user (are you allowed?) → via roles/permissions

That error:

{"status":"fail","message":"You do not have permission to perform this action."}

comes from the authorization layer.

🏗️ Folder Structure (recommended)
src/
 ├── middleware/
 │     ├── auth.middleware.ts
 │     └── role.middleware.ts
 ├── types/
 │     └── express.d.ts
 ├── routes/
 ├── controllers/
 └── utils/
1️⃣ Define Roles (TypeScript way)
// types/roles.ts
export enum Role {
  APPLICANT = "APPLICANT",
  ADMIN = "ADMIN",
  STAFF = "STAFF",
}
2️⃣ Extend Express Request (VERY IMPORTANT)
// types/express.d.ts
import { Role } from "./roles";

declare global {
  namespace Express {
    interface UserPayload {
      id: string;
      role: Role;
    }

    interface Request {
      user?: UserPayload;
    }
  }
}
3️⃣ Authentication Middleware (JWT)

This checks if the user is logged in.

// middleware/auth.middleware.ts
import { Request, Response, NextFunction } from "express";
import jwt from "jsonwebtoken";

export const protect = (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  const token = req.headers.authorization?.split(" ")[1];

  if (!token) {
    return res.status(401).json({
      status: "fail",
      message: "Not authorized, no token",
    });
  }

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET!) as any;

    req.user = {
      id: decoded.id,
      role: decoded.role,
    };

    next();
  } catch (err) {
    return res.status(401).json({
      status: "fail",
      message: "Invalid token",
    });
  }
};
4️⃣ Role-Based Authorization Middleware 🔥

This is where your error message comes from.

// middleware/role.middleware.ts
import { Request, Response, NextFunction } from "express";
import { Role } from "../types/roles";

export const restrictTo = (...allowedRoles: Role[]) => {
  return (req: Request, res: Response, next: NextFunction) => {
    if (!req.user) {
      return res.status(401).json({
        status: "fail",
        message: "Not authenticated",
      });
    }

    if (!allowedRoles.includes(req.user.role)) {
      return res.status(403).json({
        status: "fail",
        message: "You do not have permission to perform this action.",
      });
    }

    next();
  };
};
5️⃣ Use It in Routes 🚀
import express from "express";
import { protect } from "../middleware/auth.middleware";
import { restrictTo } from "../middleware/role.middleware";
import { Role } from "../types/roles";

const router = express.Router();

// Only ADMIN can access
router.get(
  "/admin-dashboard",
  protect,
  restrictTo(Role.ADMIN),
  (req, res) => {
    res.json({ message: "Welcome Admin!" }); 
  }
);

// ADMIN + STAFF
router.post(
  "/create-post",
  protect,
  restrictTo(Role.ADMIN, Role.STAFF),
  (req, res) => {
    res.json({ message: "Post created" });
  }
);

// All authenticated users
router.get("/profile", protect, (req, res) => {
  res.json({ user: req.user });
});

export default router;
⚡ Example Flow
User logs in → gets JWT:
{
  "id": "123",
  "role": "APPLICANT"
}
User hits:
GET /admin-dashboard
Middleware checks:
✅ Logged in → OK
❌ Role not ADMIN → returns:
{
  "status": "fail",
  "message": "You do not have permission to perform this action."
}
💡 Pro Tips (Real-world)
Store roles in DB (like your user object)
Add permissions later (e.g. canCreateUser)
Use constants/enums — never hardcode strings
Keep middleware reusable (like we did)
🔥 Bonus (Advanced idea)

Instead of roles, you can do:

restrictTo("CREATE_USER", "DELETE_USER")

That’s permission-based access control (PBAC) — more flexible.















Easiest Implementation in Express + TypeScript

Use express-rate-limit

Install
npm install express-rate-limit
npm install -D @types/express
✅ Basic Setup
import express from "express";
import rateLimit from "express-rate-limit";

const app = express();

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 minutes
  max: 100, // max 100 requests per IP
  message: {
    status: "fail",
    message: "Too many requests, please try again later."
  },
  standardHeaders: true,
  legacyHeaders: false
});

app.use(limiter);

app.get("/", (req, res) => {
  res.json({ message: "API running" });
});

app.listen(5000);
🔐 Better: Apply Different Limits Per Route
Login route stricter
const loginLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 5,
  message: {
    status: "fail",
    message: "Too many login attempts. Try again later."
  }
});

app.use("/api/auth/login", loginLimiter);
📦 Full TypeScript Example Structure
src/
  middleware/
    rateLimit.ts
  app.ts
middleware/rateLimit.ts
import rateLimit from "express-rate-limit";

export const apiLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 100
});

export const authLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 5
});
app.ts
import express from "express";
import { apiLimiter, authLimiter } from "./middleware/rateLimit";

const app = express();

app.use("/api", apiLimiter);
app.use("/api/auth/login", authLimiter);
⚠️ Important in Production (Very Important)

If deployed behind:

Vercel
Render
Railway
Nginx
Cloudflare

Use:

app.set("trust proxy", 1);

So Express gets the real client IP.

🧠 Where Requests Are Counted

By default, rate limiting uses:

req.ip

Meaning each IP gets its own quota.

You can customize it to use:

user ID
API key
email
token

Example:

keyGenerator: (req) => req.user.id
🔥 Production Best Practice (Redis)

Default memory store resets when server restarts and doesn’t scale across multiple servers.

Use Redis for real production apps.

Examples:

rate-limit-redis
Upstash Redis
Redis Cloud
Example with Redis Concept
Server A + Server B
Both check same Redis counter

Perfect for scaling.

🚫 What User Sees When Limited
{
  "status": "fail",
  "message": "Too many requests, please try again later."
}
🎯 Smart Limits for Real Apps
Route	Limit
Login	5 / 15 min
Register	3 / hour
Forgot Password	3 / hour
Search API	60 / min
General API	100 / 15 min
🔥 My Recommendation For You